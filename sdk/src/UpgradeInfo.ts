export interface IUpgradeInfo {
    token_level: {
        handle: string;
    };
    airdroped: {
        handle: string;
    };
}
export interface IAirdropInfo {
    airdroped: {
        handle: string;
    };
}
