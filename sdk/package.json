{
  "name": "@white-matrix/infamous-contract-sdk",
  "version": "0.0.50",
  "description": "infamous sdk aptos",
  "main": "dist/index.js",
  "scripts": {
    "build": "yarn build:clean && yarn tsc",
    "build:clean": "rm -rf dist && rm -f tsconfig.tsbuildinfo",
    "lint": "eslint \"**/*.{js,ts}\"",
    "lint:fix": "eslint --fix \"**/*.{js,ts}\"",
    "format": "prettier --check \"**/*.{ts,js}\"",
    "format:fix": "prettier --write \"**/*.{ts,js}\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aptos": "^1.3.16",
    "localforage": "^1.10.0",
    "typescript": "^4.8.4"
  }
}
